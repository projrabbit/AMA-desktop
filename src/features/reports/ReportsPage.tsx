import { Card } from '@/components/ui/Card';

export function ReportsPage() {
  return (
    <Card title="Báo cáo chấm công">
      <p>Service xuất báo cáo đã hỗ trợ tải file Excel/PDF dạng binary.</p>
    </Card>
  );
}
