import { Card } from '@/components/ui/Card';

export function AuditLogsPage() {
  return (
    <Card title="Tra cứu nhật ký hệ thống">
      <p>Nhật ký hệ thống là màn hình chỉ đọc và chỉ dành cho Admin.</p>
    </Card>
  );
}
