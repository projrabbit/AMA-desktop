import { Card } from '@/components/ui/Card';

export function ExceptionsPage() {
  return (
    <Card title="Ngoại lệ và cảnh báo">
      <p>Các trạng thái lỗi, rỗng, cấm quyền và duyệt thủ công đã có nền tảng dùng chung.</p>
    </Card>
  );
}
