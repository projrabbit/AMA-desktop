import { Card } from '@/components/ui/Card';

export function BuildingsPage() {
  return (
    <Card title="Tòa nhà và tầng">
      <p>Quản lý tòa nhà và tầng sẽ dùng endpoint trailing slash đã xác nhận từ OpenAPI.</p>
    </Card>
  );
}
