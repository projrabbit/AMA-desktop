import { Card } from '@/components/ui/Card';

export function GeofencePage() {
  return (
    <Card title="Vùng chấm công">
      <p>Foundation đã cấu hình API, quyền và trạng thái trang cho quản lý vùng chấm công 3D.</p>
    </Card>
  );
}
